export const selectInput = document.querySelector(
  "#categoria"
) as HTMLSelectElement;

export const recetario = document.querySelector("#recetario") as HTMLElement;

export const recetasContenedor = document.querySelector(
  "#recetas"
) as HTMLDivElement;

export const modalReceta = document.querySelector(
  "#modal-receta"
) as HTMLDivElement;

export const cerrarModal = document.querySelector(
  "#modal-cerrar"
) as HTMLButtonElement;

export const modalTitle = document.querySelector(
  "#modal-title"
) as HTMLHeadingElement;

export const modalContent = document.querySelector(
  "#modal-content"
) as HTMLDivElement;

export const modalButton = document.querySelector(
  "#modal-button"
) as HTMLButtonElement;

export const modalFavorito = document.querySelector(
  "#modal-favorito"
) as HTMLButtonElement;
